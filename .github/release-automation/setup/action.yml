name: Set up release automation
description: Install the pinned uv and Python runtime for release automation.
runs:
  using: composite
  steps:
    - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
      with:
        version: "0.11.29"
        python-version: "3.12"
        enable-cache: true
